import { Student } from "src/declarations/Verifier/Verifier.did"
import { Team, TeamString } from "src/declarations/Verifier/Verifier.did"
import { Activity } from "src/declarations/Verifier/Verifier.did"

import { StudentList } from "src/declarations/Verifier/Verifier.did"

export type { Student, Team, Activity, TeamString, StudentList }
